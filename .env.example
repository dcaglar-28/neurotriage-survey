# EIA Survey environment variables
# Copy to .env.local

# Optional: gate /admin with a password (local JSON store mode)
ADMIN_PASSWORD=

# Supabase (optional — app runs with local .data store when unset)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
